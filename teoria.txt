2. Чтобы добавить сокет в комнату мы используем socket.join("room1") - создает комнату и добаляет сокет
А для отправки сообщения всем пользователям группы мы используем 
this.io.to("room1").emit("event to room1", "hello room1")
пишем то что ссобщения   название события    само сообщение
будет отправелно 
всем пользователям 
этой группы
                 
4.Namespace это самый глобальный уровень в нем могут находиться room
по сути Namespace это дом, а room это крартиры в этом доме

6. При потери соединения сообщения перестанут отправляться(заморозятся), 
а при повторном подключении продолжат отправляться уже самые последние актуальные сообщения

8.Для удаления сокета socket.leave('room1') - удаляет сокет